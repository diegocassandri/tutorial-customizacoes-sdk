const axiosW = require('./../utils/axios-wrapper');

function findJobPosition(event, jobPositionId) {
    return axiosW.platformGet(event, `/hcm/payroll/entities/jobPosition/${jobPositionId}`);
}

module.exports = {
    findJobPosition
};
